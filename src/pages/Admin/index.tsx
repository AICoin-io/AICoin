import type { NextPage } from "next";
import { Text } from "@chakra-ui/react";

const Admin: NextPage = () => {
  return (
    <div>
      <Text fontSize="5xl" fontWeight="extrabold" textAlign="center" mt={10}>
        Admin
      </Text>
    </div>
  );
};

export default Admin;
